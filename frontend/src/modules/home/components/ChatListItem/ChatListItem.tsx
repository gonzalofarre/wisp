import styles from './ChatListItem.module.css'

// Placeholder de estructura — implementación real en la Fase 3
export function ChatListItem() {
  return <div className={styles.root}>ChatListItem</div>
}
